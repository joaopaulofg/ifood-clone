<project>
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.ifood.clone.parent</groupId>
        <artifactId>ifood-clone-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../ifood-clone-parent/pom.xml</relativePath>
    </parent>

    <artifactId>ifood-application</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <!-- Dependência de outro módulo -->
        <dependency>
            <groupId>com.ifood.clone.parent</groupId>
            <artifactId>ifood-domain</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
